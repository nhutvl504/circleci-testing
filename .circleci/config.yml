version: 2
jobs:
  build:
    steps:
      - checkout
      - run: |
          COMMIT_SHA=$(git rev-parse --short HEAD) \
          echo ${COMMIT_SHA}
          COMMIT_SHA=$(git rev-parse --short HEAD)
          echo ${COMMIT_SHA}
          echo $COMMIT_SHA

