machine:
  node:
    version: 0.12
    
general:
  artifacts:
    - "/usr/local/android-sdk-linux/avd" # relative to the build directory
    - "/usr/local/android-sdk-linux/system-images" # relative to the build directory
    
dependencies:
  pre:
    - nvm install 4.0
    
test:
  override:
    - android list avd
    - android list target
