machine:
  node:
    version: 0.12
    
checkout:
  post:
    - mkdir ~/.pip/cache
    
dependencies:
  cache_directories:
    - ~/.pip/cache
    
  pre:
    - nvm install 4.0
    
test:
  override:
    - node --version
    - nvm use 4.0; node --version
