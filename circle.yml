machine:
  pre:
    - echo y | sudo add-apt-repository ppa:ondrej/php
    - sudo apt-get update
    - sudo apt-get install && sudo apt-get install php7.0
    
dependencies:
  pre:
    - phpenv install --releases
    
test:
  override:
    - php --version
