machine:
  node:
    version: 5.1.0
    
dependencies:
  pre:
    - echo "None, for now"
    - sudo ls -l /usr/bin/
    
test:
  override:
    - nvm use 5.0.0; node --version; npm --version
    - nvm use 5.1.0; node --version; npm --version
