#machine:
#  python:
#    version: 2.7.11
    
dependencies:
  pre:
    - cp .circlerc ~/.circlerc && chmod +x ~/.circlerc
    - ls -lah ~/.circlerc
  post:
    - sudo dpkg --get-selections > $CIRCLE_ARTIFACTS/listing.txt
    - chmod +x getBuildInfo.sh
    - ./getBuildInfo.sh
    
test:
  override:
    - python check_shared.py
    - echo "done"
