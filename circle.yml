machine:
  node:
    version: 5.1.0
    
dependencies:
  pre:
    - echo "None, for now"
    
test:
  override:
    - nvm install 4.4; nvm use 4.4; node --version; npm --version
    - nvm install 4.4.7; nvm use 4.4.7; node --version; npm --version
