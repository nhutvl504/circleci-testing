machine:
  pre:
    - sudo add-apt-repository ppa:ondrej/php
    - sudo apt-get update
  php:
    version: 7.0.5
    
dependencies:
  pre:
    - echo "None, for now"
    
test:
  override:
    - php --version
