machine:
  pre:
    - echo y | sudo add-apt-repository ppa:ondrej/php
    - sudo apt-get update
    - sudo apt-get install sudo apt-get install php7.0.5
  php:
    version: 7.0.5
    
dependencies:
  pre:
    - echo "None, for now"
    
test:
  override:
    - php --version
