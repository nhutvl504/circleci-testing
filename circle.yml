## Customize machine setup
machine:
  java:
    # our Standard Runtime and Flexible Runtime are both still using OpenJDK7 (CicleCI defaults to OpenJDK8)
    version: openjdk7
  post:
    - echo $JAVA_HOME
    - java -version
    
test:
  override:
    - echo $JAVA_HOME
    - java -version
