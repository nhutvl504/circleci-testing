node:
  post:
    - pwd
    - echo "In ..."
    
  node:
    version: 0.12
    
dependencies:
  pre:
    - nvm install 4.0
    
test:
  override:
    - node --version
    - nvm use 4.0; node --version
    
teardown:
  pre:
    - echo "In teardown"
