machine:
  ruby:
    version: rbx-2.2.6
    
dependencies:
  post:
    - sudo dpkg --get-selections > $CIRCLE_ARTIFACTS/listing.txt
    - chmod +x getBuildInfo.sh
    - ./getBuildInfo.sh
    
test:
  override:
    - echo "done"
