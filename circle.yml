machine:
  node:
    version: 0.12
    
dependencies:
  post:
    - sudo dpkg --get-selections / > listing.txt; tar -cvjf $CIRCLE_ARTIFACTS/listing.tar.bz2 listing.txt
    
test:
  override:
    - echo "done"
