machine:
  node:
    version: 5.1.0
    
dependencies:
  pre:
    - echo "None, for now"
    
test:
  override:
    - npm install -g mocha
    - mkdir $CIRCLE_TEST_REPORTS/junit
    - mocha -R xunit --reporter-options output=$CIRCLE_TEST_REPORTS/junit/filename.xml
