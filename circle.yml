machine:
  node:
    version: 0.12
    
general:
  artifacts:
    - "/.android" # relative to the build directory
    
dependencies:
  pre:
    - nvm install 4.0
    
test:
  override:
    - node --version
    - nvm use 4.0; node --version
