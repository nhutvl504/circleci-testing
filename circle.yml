machine:
  node:
    version: 8
dependencies:
  pre:
    - npm i --global npm@5.5
    
dependencies:
  pre:
    - echo "None, for now"
    
test:
  override:
    - node --version; npm --version
    - nvm use 8; node --version; npm --version
